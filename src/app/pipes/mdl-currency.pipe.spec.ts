import { MdlCurrencyPipe } from './mdl-currency.pipe';

describe('MdlCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new MdlCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
